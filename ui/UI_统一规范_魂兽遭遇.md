# 魂兽遭遇统一 UI 规范

> **文件职责：** 只定义魂兽遭遇开始、回合结算与结束卡的玩家可见字段、顺序和排版。
>
> **权威范围：** UI schema；不拥有战斗公式、技能数值、经验、收益、遭遇耗时或状态转换规则。
>
> **读取策略：** 仅魂兽遭遇输出时读取。

所有数值和结果必须由 `10 / 11 / 12 / B00 / R00 / E00 / S00` 等当前 owner 先计算，再注入模板。

## 1. 遭遇开始卡

```markdown
## 【<行动标题｜新遭遇>】

<必要现场叙事>

### 【<魂兽显示名>】

| 单位 | 等级/年份 | HP | 魂力 | ATK | DEF |
|---|---|---:|---:|---:|---:|
| <参战单位> | <值> | <值> | <值> | <值> | <值> |

**<玩家/伙伴单位｜武魂>**
- 普通攻击：<从战斗 owner 注入的玩家可见效果>
- <当前已拥有技能及玩家可见数值>

**<魂兽｜攻击白值>**
- 普通攻击：<基础白值>
- <当前公开技能白值/效果>
- 击杀经验：<KILL_XP>
- 猎获收益：<当前 owner 给出的值>

### 【<当前轮次｜林昭宁行动>】

行动顺序：**<从 COMBAT_STATE/Kernel 注入>**
当前 EXP：**<值>**
游戏时间：**<WORLD_TIME>**
现实活动：**<状态>**

**林昭宁行动。**
```

开始卡不自行冻结、重抽或计算魂兽；它只展示已冻结状态。

## 2. 回合结算卡

```markdown
## 【第<N>轮结算】

<已结算行动与结果叙事>

- <受影响单位> HP：**<当前> / <最大>**
- <如有魂力变化则显示>

### 【第<N+1>轮｜林昭宁行动】

- 林昭宁：**<HP>/<最大> HP｜<魂力>/<最大> 魂力**
- <必要伙伴/敌方状态>
- EXP：**<当前> / <升级所需>**

**林昭宁行动。**
```

伙伴与敌方是否已自动续转由 `00A K4` 决定，UI 不维护第二套回合规则。

## 3. 遭遇结束卡

```markdown
### 【<遭遇结果标题>】

<必要结果叙事>

- 击杀经验：**<本次真实结算值>**
- 猎获收益：**<本次真实结算值>**
- 林昭宁：**<HP>/<最大> HP｜<魂力>/<最大> 魂力**
- <必要伙伴状态>
- EXP：**<当前> / <升级所需>**
- 金钱：**<当前>**
- 地点：**<当前地点>**
- 遭遇结果：**<ENCOUNTER_RESULT>**
- 游戏时间：**<结算前> → <结算后>**
- 现实活动：**<状态>**

**林昭宁可以自由行动。**
```

没有真实击杀时，击杀经验/猎获收益按领域 owner 返回值显示；遭遇耗时只展示 `11` 已计算并由 `12` 提交后的结果，本文件不保存固定时长。
