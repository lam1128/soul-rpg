# 魂师修炼RPG｜状态与存档

> 本文件定义动态状态结构与生命周期，但不保存某次冒险的当前具体值。当前具体值由 registry 登记的稳定 Git state owner 保存；外部 checkpoint 只有通过本文件校验后才能作为显式导入候选。

> **文件职责：** 状态 schema、现实活动/关系/战斗/世界字段语义、Git current state 校验、checkpoint 导入/恢复、迁移、会话候选态原子提交与兼容导出协议。
> **权威范围：** 动态状态“长什么样、如何恢复、如何迁移、如何运行提交、如何持久化”的唯一协议主源。
> **不负责：** 不在本规则文件内保存某次冒险的当前具体值；当前持久值只存在于 registry 登记的 `state/current/SOUL_STATE_V1.yaml`，`runtime_working_state` 只是单轮事务候选。
> **写权限：** 本文件执行 manifest 已授予的 state access；普通运行不得越过候选态边界，稳定 Git state 只能经 registry 登记的 `STATE_COMMIT` 写入；兼容 checkpoint 只由登记的存档 Skill 导入/导出/迁移。
> **读取策略：** 先读当前事务需要的 schema / 协议段；具体动态值从工作态或合法存档读取。

## 1. 存档头

```text
SCHEMA_VERSION: 2.1
CHECKPOINT_FORMAT_VERSION: SOUL_STATE_V1
ADVENTURE_ID
STATE_REV
ACTIVE_TRANSACTION_ID
LAST_COMMITTED_TRANSACTION_ID
PHASE: CHAR_CREATE / ADVENTURE / REAL_ACTIVITY / COMBAT / COMPANION_EPISODE
CHAR_CREATE_STEP: NAME / MARTIAL_SOUL / REVIEW / GUIDE / DONE
PLAYER_GUIDE_SHOWN: NO / YES
SAVED_AT_REAL_TIMESTAMP_WITH_OFFSET
USER_TIMEZONE
MIGRATION_PROVENANCE: null / {FROM_SCHEMA_VERSION, FROM_STATE_REV, MIGRATION_KIND, GAMEPLAY_FACTS_CHANGED?, CORRECTION_SUMMARY?}

# 仅兼容 checkpoint 可携带、canonical Git state 不以其为权威：
RUNTIME_RELEASE: optional
RUNTIME_PACKAGE: optional
RUNTIME_PACKAGE_SHA256: optional
```

`PLAYER_GUIDE_SHOWN` 只记录本次冒险是否已经完成开局指南展示；再次查看指南不改变该值、不推进世界时间，也不建立新的世界事实。旧存档缺少该字段时：若已处于正式冒险，则保守补为 `YES` 并允许玩家随时主动再次查看；若仍在角色创建，则补为 `NO` 并进入 `GUIDE`。

`RUNTIME_RELEASE / RUNTIME_PACKAGE / RUNTIME_PACKAGE_SHA256` 是旧 checkpoint / 兼容导出时期的包绑定元数据，**不是 Git current state 的源码或运行权威**。Git source mode 永远以当前 `main` HEAD + `runtime/registry.json` 为运行规则；canonical current state 可以在无游戏事实变化、无 `STATE_REV` 变化的序列化规范化中移除这些字段，普通后续提交不得因为当前使用哪个 runtime ZIP 而重新写回它们。外部 checkpoint 导入时可以读取这些字段做兼容诊断，但不得据此覆盖当前 registry、route、owner 或源码版本。

## 1A. Git current state 与 checkpoint import

### 四层状态模型

- `12_状态存档.md`：schema / 恢复 / 迁移 / 提交 / 导出协议 owner。
- `state/current/SOUL_STATE_V1.yaml`：当前稳定冒险状态 owner；由 registry `state_model.current_state_owner` 发现并随 Git `main` 持久化。
- `runtime_working_state`：从 current state 复制出的单轮事务候选；只在本轮计算/校验中存在，不能成为第二持久化主源。
- 外部 `SOUL_STATE_V1` ZIP：immutable compatibility artifact，只用于显式 `LOAD_SAVE` 导入/恢复或 `SAVE_EXPORT` 便携导出；不再作为普通启动的 current source。

### checkpoint import 候选消费

外部候选合法性、冒险筛选、revision/time 排序与冲突失败顺序只读取 `manifest.external_sources.checkpoint_import`。本文件只负责对 resolver 选出的候选执行 schema 校验、必要迁移与恢复；不维护第二套排序算法。

### 工作态与 Git 持久化

新窗口在 Git 源码可用时直接从 current state owner 建立 `runtime_working_state`。普通游戏 route 只能形成一次最终候选；通过 schema、事务、防重复和领域校验并完成本文件 §8A/§8B 的 canonical 序列化规范化后，必须调用 `manifest.internal_services.state_commit` 原子写回 `state/current/SOUL_STATE_V1.yaml`。写入后的 Git tree 才是下一轮 current state。不得依赖聊天记忆补齐未提交进度。

`STATE_REV` 只表示兼容 checkpoint lineage revision，不因普通游戏 Git state commit 自动增加。Git commit SHA 才是每次稳定状态变更的持久历史身份。只有明确 `SAVE_EXPORT` 或需要产生新 checkpoint lineage revision 的显式迁移才更新 `STATE_REV`；普通“存档 / 保存进度”的 `SAVE_CURRENT` 只是确认 Git current state 已持久化，不创建新 revision。

### 旧 schema → 2.1 迁移

合法 `SOUL_STATE_V1` 的旧 schema 可按当前 `STATE_MIGRATION` dispatcher 迁移。迁移协议必须满足：

- 保留全部既成游戏事实与数值；
- 不推进 `WORLD_TIME`，不发EXP/金钱/奖励；
- 对缺失的 `REAL_ACTIVITY_SESSION.NARRATIVE_OVERLAY` 补 `NONE`；
- 对缺失的现实活动区间Ledger按当前兼容规则初始化；
- 从 schema 2.0 迁移时新增 `PLAYER_DISCOVERY_STATE`：不得由 `RELATION_STATE.ACQUAINTED` 直接推导；只能依据 checkpoint 内已持久化的玩家可见直接见面/重逢证据确定 `MET_IN_PLAY`，证据不足时保守为 `false`；锚定伙伴若已有明确玩家可见同行/互动事实可确定为 `true`；
- 若旧版 schema 2.0 checkpoint 含 `SLEEP_WORLD_SYNC` / `SLEEP_WORLD_SYNC_APPLIED`，只作为兼容残留忽略；不得把这些旧字段恢复为运行能力，也不得据此推进世界时间或恢复；
- 更新 schema / runtime binding，并生成新的 `STATE_REV`；
- 保存迁移来源 provenance，但不得把旧运行包恢复指令继续作为当前操作步骤。

## 2. 玩家机械真值

```text
PLAYER_SHEET:
  ACTOR_ID
  NAME
  SEX
  AGE
  LEVEL
  TITLE
  XP_CURRENT
  XP_TO_NEXT
  BOTTLE_NECK_BANKED_XP
  MARTIAL_SOUL_ID
  HP / MAX_HP
  SOUL_POWER / MAX_SOUL_POWER
  ATK / DEF
  SOUL_RINGS[]
  SOUL_SKILLS[]
  MONEY
  MEDICINE_INVENTORY
  MEDICINE_CARRY_CAP
  LAST_MEAL_RECOVERY_WORLD_TIME
```

新冒险初始化值由 `11 / 10A / S00 / E00` 等领域 owner 提供；`12` 只负责按本 schema 接收并提交，不复制开局常量。

## 3. 现实生活成长会话

```text
REAL_ACTIVITY_SESSION:
  STATUS: NONE / ACTIVE
  TYPE: NONE / TRAINING_STUDY / WORK / SLEEP
  START_AT_REAL_TIMESTAMP_WITH_OFFSET
  START_STATE_REV
  NARRATIVE_OVERLAY: NONE / TRAINING_STUDY / WORK

REAL_ACTIVITY_DAILY_LEDGER:
  LOCAL_DATE -> {
    WORK_CREDITED_MINUTES,
    SLEEP_CREDITED_MINUTES
  }

REAL_ACTIVITY_INTERVAL_LEDGER[]:
  ACTIVITY_ID
  TYPE: TRAINING_STUDY / WORK / SLEEP
  SOURCE: LIVE / RETRO
  START_AT_REAL_TIMESTAMP_WITH_OFFSET
  END_AT_REAL_TIMESTAMP_WITH_OFFSET
  ACTUAL_FULL_MINUTES
  CREDITED_MINUTES_BY_LOCAL_DATE
  COMMITTED_TRANSACTION_ID
```

- 同一时刻最多一个现实活动会话为 `ACTIVE`。
- `REAL_ACTIVITY_INTERVAL_LEDGER` 是区间防重复真值；实时结束与合法补记都追加记录。
- 合法补记的时间解析、活动类型与控制语义读取 `13`；奖励倍率、每日上限与经验计算读取 `10`。
- 缺失区间 Ledger 的旧 schema 2.0 checkpoint 可在工作态按兼容规则初始化；无法证明与旧按日计奖记录不重叠时，补记必须失败关闭。
- 现实活动 route 对哪些动态字段具有写权限由当前 route + `10/11` 的领域边界共同决定；`12` 只执行校验和原子提交，不复制奖励或世界规则。
- 旧 `REAL_TRAINING_SESSION`、`SLEEP_WORLD_SYNC` 等仍有合法兼容消费者时只用于恢复/迁移，不能恢复成新的运行能力。

## 4. 伙伴

```text
COMPANION_ROSTER
ANCHOR_COMPANION_STATE
COMPANION_PROFILES:
  ACTOR_ID -> LEVEL/HP/MAX_HP/SOUL_POWER/MAX_SOUL_POWER/ATK/DEF/RINGS/SKILLS/CURRENT_AVAILABILITY
```

伙伴等级字段按 `10 §8` 的同步规则更新；`12` 只保存结果。

## 5. 关系与玩家发现

每名核心人物的**世界内关系**单独保存：

```text
RELATION_STATE[ACTOR_ID]:
  ACQUAINTED
  STABLE_COMPANION
  EPISODE_AFFINITY_TOTAL: 0..100
  CHAPTER_BEST_SCORE: {1..5 -> 0..20}
  ROMANCE_ELIGIBLE: true/false
  ROMANCE_ACTIVE: true/false
  ROMANCE_FLAGS: []
```

`ACQUAINTED` 只表示主角林昭宁在世界内是否认识该人物；开局历史关系可以使其在冒险开始前已经为 `true`。

玩家在当前冒险是否已经亲自经历该角色的正式登场，使用独立状态：

```text
PLAYER_DISCOVERY_STATE[ACTOR_ID]:
  MET_IN_PLAY: true/false
```

`MET_IN_PLAY` 的成立语义读取 `11`：必须已经发生玩家可见、足以确认身份的直接见面或重逢。该字段不是关系好感，也不能由 `ACQUAINTED=true` 自动推出。

关系与玩家发现字段的世界语义读取 `11`；篇章通用进入/关系门消费规则读取 `C00`，当前章具体旗标/结果语义只读当前单章文件；`12` 只保存结果。

## 6. 伙伴篇章

```text
COMPANION_EPISODE_STATE:
  ACTOR_ID:
    CHAPTER_n:
      UNLOCKED
      FIRST_CLEAR_DONE
      CANON_ENDING_ID
      FIRST_CLEAR_REWARD_CLAIMED
      BEST_SCORE
      BEST_ROMANCE_FLAGS
      REPLAY_COUNT
```

CG 收藏继续**不增加独立状态字段**。每章两个CG槽都由现有篇章进度派生：

- `基础CG` 是否收录只由该章 `FIRST_CLEAR_DONE` 推导；未首通时基础槽为未解锁。
- `高好感CG` 是否收录只由该章历史最高分 `BEST_SCORE`（与 `RELATION_STATE.CHAPTER_BEST_SCORE` 的对应章值）按 `C00 §0` 的高好感CG门槛推导。
- 一旦历史最高分达到高好感门槛，高版与基础版均视为已收录；之后重放得到较低分不会反锁收藏。
- 先以较低分完成、后续重放把历史最高分提高到门槛时，高版自动补收录；不重复发首通奖励，也不需要迁移或新增CG专用持久化字段。

这一定义只服务收藏展示；CG画面版本、门槛与章内出现规则归 `C00 + 当前单章故事卡`，`12` 不复制其业务常量。

篇章字段的通用提交/重放边界读取 `C00`，当前章具体结果语义读取 manifest 命中的一个单章文件；`12` 负责保存已计算结果与防重复字段。

## 7. 战斗

```text
COMBAT_STATE:
  ROUND
  PHASE_SLOT: PLAYER / COMPANION_1 / COMPANION_2 / COMPANION_3 / ENEMY_n
  PARTY[]
  ENEMIES[]
  ALL_PUBLIC_STATS
  STATUS_EFFECTS
  ESCAPE_AVAILABLE
  SOUL_RING_TARGET_IF_ANY
```

`ALL_PUBLIC_STATS` 的具体公开边界读取 `10/13`；`12` 只保存当前战斗所需状态。

## 8. 世界

持续保存：
- `WORLD_TIME`：必须为精确 `Day N | HH:MM`；禁止 `清晨锚点`、`RELATIVE_ANCHOR` 等模糊/临时字段进入正式 current state / checkpoint
- `PLAYER_LOCATION`
- `CURRENT_SCENE`
- `NPC_STATE`
- `WORLD_GEOGRAPHY`
- `ACTIVE_HUNT_INSTANCE`
- `KILLED_SOUL_BEAST_LOG`
- `ENCOUNTER_LOG`
- `KNOWN_SOUL_BEASTS`
- `WORLD_FACTS`
- `RECENT_EVENT_LOG`

### 8A. 历史信息压缩边界

为避免 canonical Git state 随游玩时长线性膨胀，历史字段按职责分层：

- `WORLD_FACTS` 只保留**仍对未来连续性有用的耐久事实**；已经被后续事实明确取代的旧表述应被替换，不并列保留互相冲突的过程版本。调试、规则检查、纠错过程和“先中止后重做”之类施工痕迹不得进入耐久事实。
- `RECENT_EVENT_LOG` 只负责近期叙事回顾，按世界时间排序。每次形成将要写入 canonical Git state 的最终候选时，最多保留最近 **48** 条；更早事件若其后果已由当前状态、`WORLD_FACTS`、关系/篇章状态、击杀记录或遭遇记录完整承接，可在同一次无收益规范化中移除。
- `KILLED_SOUL_BEAST_LOG`、`ENCOUNTER_LOG`、一次性奖励记录和各类防重复 ID 属于机械审计记录，不得为了压缩而删除仍承担防重复/恢复校验作用的条目。
- 历史压缩只允许删除重复、被取代或已由其他权威字段完整承接的叙事记录；不得改变 EXP、金钱、物品、世界时间、关系、篇章、任务、战斗状态或任何一次性结算事实。
- 旧 checkpoint 的超长 `RECENT_EVENT_LOG` 仍可正常作为 `LOAD_SAVE` 输入候选；若最终要物化为 canonical Git state，必须在该次 `STATE_COMMIT` 前按本节规范化。`SAVE_EXPORT` 只导出已经合法的 current state，不再承担“等到导出时才清理”的职责。

### 8B. 派生缓存、兼容包字段与无消费者字段

当前权威世界字段是 `WORLD_TIME` 与 `PLAYER_LOCATION`。旧 checkpoint 中若同时存在 `WORLD_DAY / CLOCK / PLAYER_LOCATION_DISPLAY`，它们只作为**派生缓存**读取：必须能从权威字段一致推导；不一致时以权威字段为准。只要候选将写入 canonical Git state，就应在提交前移除这些重复缓存；不得把清理推迟到未来某次 `SAVE_EXPORT`。

旧 checkpoint 还可能含 `ONE_TIME_REWARD_RECORDS / HIDDEN_WORLD_STATE / PENDING_ACTIONS` 等历史容器。当前运行包没有为这些容器登记独立消费者，因此：

- 若容器为空、false/null 或只含重复说明性 `NOTE/NOTES`，`LOAD_SAVE` 可忽略；若候选被接受为 canonical Git state，则在该次提交前不再携带。
- 若其中存在**无法由当前关系/篇章/奖励防重复字段、当前场景、ACTIVE事务或其他权威状态完整承接的非空语义数据**，不得静默删除，必须失败关闭并先通过明确维护/迁移把该事实映射到当前 owner。
- 说明性 NOTE/NOTES 不得作为游戏事实主源，也不得反向覆盖当前场景、遭遇、关系、奖励或迁移 provenance。

`RUNTIME_RELEASE / RUNTIME_PACKAGE / RUNTIME_PACKAGE_SHA256` 同样属于兼容包绑定残留：外部 checkpoint 可以携带以帮助诊断来源，但 canonical Git state 不消费这些字段决定规则或版本。普通运行最终候选与接受后的外部导入候选都应在写入 canonical state 前移除这类包绑定字段；这只是序列化规范化，不改变游戏事实、不增加 `STATE_REV`。明确 `SAVE_EXPORT` 如需把兼容来源元数据写进外部 snapshot，可在导出副本中生成，但不得再反向写入 canonical current state。

上述规范化都属于 canonical 序列化去冗余，不产生新的游戏收益；对当前已存在的惰性兼容字段，也允许在下一次正常 `STATE_COMMIT` 时自然清理，无需为此单独创建 gameplay migration revision。

具体魂兽个体生成后保存 `INSTANCE_ID / SPECIES_ID / YEARS / HP / SOUL_POWER / ATK / DEF / SKILLS / KILL_XP / LOCATION / ALIVE`。普通随机遭遇还必须能由当前 `LOCATION/SUBZONE + R00普通表 + B00物种范围/公式 + E00收益` 反向验证；验证通过后才允许冻结。

历史兼容：若旧 checkpoint 已经结算完成的实例来自已确认运行错误、无法映射到当前 `B00/R00`，明确迁移时可保留该**已结束历史实例**，但必须标记 `LEGACY_RUNTIME_ANOMALY: YES`；该实例只用于历史恢复/防重复，不得进入候选物种池、不得扩写成新的 B00 物种、不得再次生成。ACTIVE 遭遇不得使用此兼容豁免。

魂兽个体死亡记录还必须保存 `HUNT_PAYOUT / HUNT_PAYOUT_APPLIED`。魂兽首次死亡时按 `DM_ONLY_E00 §2` 结算猎获收益；`HUNT_PAYOUT_APPLIED=YES` 后禁止重复发钱。

每个魂兽遭遇实例必须保存 `ENCOUNTER_ID / ENCOUNTER_STATUS / ENCOUNTER_RESULT / ENCOUNTER_TIME_APPLIED / START_WORLD_TIME / END_WORLD_TIME`。遭遇结束时的世界时间增量只由 `11 §8A` 计算；`12` 只保存结算标记并防止同一 `ENCOUNTER_ID` 重复应用。

魂兽死亡记录只负责个体死亡与击杀/收益防重复；不得承担遭遇时间 owner 职责。

## 9. 会话工作态原子提交

一次玩家输入只允许一次最终运行提交；HP、魂力、经验、钱、药、时间不能重复扣/加。现实活动结束/切换/补记也只能结算一次；已写入 `REAL_ACTIVITY_INTERVAL_LEDGER` 的现实区间不得再次结算，`REAL_ACTIVITY_DAILY_LEDGER` 继续负责每日计奖上限。

跨消息暂停时保持同一个 `ACTIVE_TRANSACTION_ID`。正式游戏 route 的提交对象先是 `runtime_working_state` 候选，权限为 `RUNTIME_MUTATION`；候选通过领域/schema校验后先执行 §8A/§8B 的 canonical 序列化规范化，再经 `STATE_COMMIT` 写回 stable Git state。普通运行提交不会生成/改写外部 checkpoint，也不会自动增加 `STATE_REV`。

`SAVE_CURRENT` 不创建新的工作态候选、不重放上轮事务、不为了“再存一次”重复提交；它只读取已持久化的 canonical state 并确认保存状态。若当前有尚未完成/提交失败的原事务，不得用 `SAVE_CURRENT` 掩盖该错误。

若当前消息只是帮助、查看、`SAVE_CURRENT` 或项目外讨论，状态权限保持 `READ_CONSUMER`。

## 10. 正式持久化、导出与恢复

### SAVE_EXPORT

`SAVE_EXPORT` 只在用户**明确要求外部便携产物**时命中，例如“导出存档 / 导出备份 / 生成兼容存档 / 生成 checkpoint / 导出 ZIP / 下载存档”。单独的“存档 / 保存进度 / 存一下档”不得命中本事务，而由 `13 §2C SAVE_CURRENT` 处理。

本节只定义兼容导出协议，不定义外部 Skill 的执行步骤。Git current state 在导出前已经是正式持久化真值；合法导出必须满足：

- 输入是 `state_model.current_state_owner` 的当前合法状态；不能从聊天记忆或未提交候选导出；
- `NEXT_STATE_REV = 当前 Git state 的 STATE_REV + 1`；若另有用户明确选择的同冒险兼容 checkpoint 候选，只能用于冲突检查，不能反向覆盖 current Git state；
- 先形成只含允许兼容 lineage 元数据变化的 export candidate：更新 `STATE_REV / SAVED_AT`，不改变游戏事实。若当前协议要求 lineage 与 canonical state 同步，则验证后可通过 `STATE_COMMIT` 同步这类 checkpoint lineage 元数据；**不得**把本次导出 ZIP 的文件名、ZIP hash、runtime package binding 或旧包恢复指令写回 canonical current state；
- 输出文件名为 `魂师存档_<主角名>_V1_REV<NEXT_STATE_REV>_SOUL_STATE_V1.zip`；
- ZIP 必须包含完整 `SOUL_STATE_V1.yaml`、`PLAYER_SAFE_VIEW.md`、`恢复说明.txt`；外部 snapshot 可按兼容协议携带导出来源说明，但运行时规则仍只认导出时的 Git source identity/当前 registry，不认旧包字段为主权威；
- ZIP 从磁盘重开验证通过后才是合法便携 checkpoint，但它仍是 Git state 的导出物，不成为项目主权威；
- `SAVE_EXPORT` 本身不推进游戏世界时间，不产生 EXP、金钱、关系或剧情变化，也不承担 canonical state 的日常压缩/去冗余职责。

### STATE_MIGRATION

本节只定义迁移允许改变什么。只有用户明确授权旧 schema / 旧 release 迁移时才允许生成迁移 revision；默认必须证明除 schema、兼容字段、provenance、保存时间与允许的 checkpoint lineage 元数据外，游戏事实未变化。

**确定性运行错误修复例外：** 若用户明确授权把历史 checkpoint 校正到当前协议，只允许修复能够由现行权威主源**唯一推导**的派生错误。具体字段合法性、世界时间规范、历史异常实例兼容与清理方式统一读取本文件对应 schema/兼容章节，不在迁移章节维护第二套例子表。

**用户确认事实纠错：** 若用户明确指出某个已持久化游戏事实记录错误，并明确给出正确事实，`STATE_MIGRATION` 可以只修改该用户点名事实及由它直接决定的派生资格/可用性字段。不得把一次确认扩张为未点名人物、事件或历史的推测；已经完成的篇章结局、得分、奖励和防重复标记保持原值，除非用户另行明确纠正且当前 owner 能证明修改边界。该迁移必须记录 `GAMEPLAY_FACTS_CHANGED=true` 与具体 `CORRECTION_SUMMARY`。

任何纠错迁移都不得新发EXP/金钱/物品、不得重抽 ACTIVE 遭遇、不得推进世界时间，也不得猜测无法确定的历史。必须在 `MIGRATION_PROVENANCE` 记录来源 revision、迁移种类与纠错摘要。

### LOAD_SAVE / CHECKPOINT_IMPORT

Git 源码可用时，普通“读档 / 继续游戏 / 继续 / 恢复游戏”在**没有点名外部 checkpoint/ZIP**时不执行本事务，而是直接读取 current state owner。只有用户明确要求“导入旧存档 / 恢复某个外部 ZIP / 切换到某个 checkpoint / 加载某个外部存档”时才执行 `LOAD_SAVE`：当前 Git `main` 的规则/registry 始终是运行权威；外部 checkpoint 仅作为输入候选。

选中候选后按 schema 校验；若需要 schema/runtime migration，先走本节 `STATE_MIGRATION` 允许边界；在最终通过 `STATE_COMMIT` 物化为 canonical state 之前必须执行 §8A/§8B 的规范化，包括去除无权威的 runtime package 绑定和可安全删除的旧缓存/空容器。导入本身不发奖励、不推进世界时间。非 registry 登记状态格式不得替代正式 checkpoint，`PLAYER_SAFE_VIEW.md` 也不得当完整状态。

仅有兼容 runtime ZIP、没有 Git checkout 时，兼容 manifest 的 `generated_view.state_projection` 允许退回外部 checkpoint → 会话工作态模式；该 fallback 不改变 Git-first 项目的主权威定义，也不得把裸“存档”伪装成已经写入 Git。

## 11. State Ledger

普通状态摘要只规定字段槽位，不保存示例常量：

```text
[STATE LEDGER]
姓名｜Lv.<LEVEL> <TITLE>
EXP：<XP_CURRENT> / <XP_TO_NEXT>
HP：<HP> / <MAX_HP>
魂力：<SOUL_POWER> / <MAX_SOUL_POWER>
金钱：<MONEY>
药品：<MEDICINE_INVENTORY>
同行：<当前玩家可见同行者>
```

具体展示值读取当前工作态；战斗中的紧凑显示读取 `13/UI`。
