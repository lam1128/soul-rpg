# 日常恢复、菜单与主页面统一 UI 规范

> **文件职责：** 只定义经济菜单、恢复方式、恢复结算、游戏主页面与CG大厅的玩家可见字段、顺序和排版。
>
> **权威范围：** UI schema；不拥有菜品/药品名称、价格、恢复量、固定耗时、携带上限、冷却、可用性规则或CG解锁算法。
>
> **读取策略：** 仅命中 `economy_transaction`、`recovery`，或 `HELP_READONLY / STATUS_READONLY` 的主页面、导航、CG大厅请求时读取对应章节。

所有业务值必须在渲染前由当前 route 从 `E00 / 10 / 11 / 12` 等 owner 取出并注入；本文件不得保存这些值的副本。

## 1. 交易菜单

适用于饭店、家中做饭、药铺等已经由交互层确认需要玩家选择项目的场景。

```markdown
### 【<菜单标题>】

| 项目 | 价格/成本 | 效果 | 当前可用性 |
|---|---:|---|---|
| <从领域 owner 注入的项目> | <值> | <值> | <可用/原因> |

当前金钱：**<runtime_working_state.MONEY>**
<按当前交易需要显示库存、人数、携带上限等玩家字段>

**<当前需要玩家做出的选择>**
```

规则：

- 行数、项目名、价格、效果和交易耗时全部从业务 owner 注入。
- 展示菜单本身不预结算；是否已经产生移动/时间后果由当前 route 的工作态结果决定。
- 不得因 UI 空间自行删项、改价、合并档位或创造缺货。

## 2. 恢复方式

玩家询问恢复方案、或当前场景需要从多种合法恢复方式中选择时：

```markdown
### 【恢复方式】

| 方式 | 游戏时间 | HP变化 | 魂力变化 | 消耗/限制 | 当前可用性 |
|---|---:|---:|---:|---|---|
| <从恢复 owner 注入> | <值> | <值> | <值> | <值> | <可用/原因> |

当前状态：
- 林昭宁：**<HP>/<MAX_HP> HP｜<SOUL_POWER>/<MAX_SOUL_POWER> 魂力**
- <必要同行者状态>
- 地点：**<PLAYER_LOCATION>**
- 金钱：**<MONEY>**
- 药品：**<MEDICINE_INVENTORY>**

**林昭宁可以选择一种当前合法方式。**
```

UI 只展示当前 owner 返回的合法选项与值；现实活动入口是否属于恢复选项由 `13` 决定，本文件不判断。

## 3. 交易结果卡

```markdown
### 【交易完成｜<类型>】

- 游戏时间：**<如发生变化则显示>**
- 金钱：**<前> → <后>**
- 库存：**<如发生变化则显示>**
- <其他由本次交易真实改变的玩家字段>

<必要现场叙事>
```

## 4. 恢复结算卡

```markdown
### 【恢复结算｜<方式>】

- 游戏时间：**<如发生变化则显示>**
- 林昭宁 HP：**<前> → <后> / <最大>**
- 林昭宁魂力：**<前> → <后> / <最大>**
- <参与者或消耗字段，如有>

<必要现场叙事>

**林昭宁可以自由行动。**
```

没有变化的字段可以省略。UI 不得根据模板自行计算任何结果；所有前后值都必须来自本轮已校验候选/已提交工作态。


## 5. 游戏主页面｜只读导航

适用于玩家明确输入“主页面 / 游戏主页 / 总菜单 / 导航 / 我现在能做什么”等导航请求。是否命中该帮助意图只由 `13 §2A` 识别；本节只负责展示结构。

主页面目标：一眼看到**当前状态、当前行动、可做事项、可玩篇章和社交线索**。打开主页面只读取当前工作态与领域 owner 注入结果。

推荐渲染结构：

```markdown
### 【魂师修炼RPG｜主页面】

**<游戏日/时间>｜<地点>**
林昭宁 **Lv.<等级>｜HP <HP>/<MAX_HP>｜魂力 <SOUL_POWER>/<MAX_SOUL_POWER>｜<金钱>**
同行：**<当前同行；无同行时省略本行>**

**当前：** <当前事件或当前自由行动状态>

**可做：** <3–6个当前最相关、可直接执行的行动，用“｜”分隔>

**篇章：** <只列当前真实可进入的伙伴篇章；若当前没有，则给一条正向解锁方向>

**社交：** <一句当前地点可用的公开认识/互动线索；没有特别线索时省略>

**查看：** 状态｜伙伴｜篇章｜CG大厅｜存档
```

渲染细则：

1. 全页保持紧凑，通常控制在 8–12 行，不使用大表格。
2. 只展示当前真实可用或对下一步有帮助的项目，优先使用动词开头的正向短语。
3. 省略内部规则说明、限制条款、实现解释、教学示例和重复提示。
4. “篇章”只消费 `C00` 实时校验后可进入的结果；该人物尚未在本局完成玩家可见正式见面/重逢时，不进入可玩列表。
5. “社交”从 `11` 注入一条当前最有用的公开线索，按当前关系自然表现为初识、重逢或继续互动。
6. 当前存在 ACTIVE 战斗、遭遇或伙伴篇章时，“当前”与“可做”优先给出继续该事件的入口。
7. 所有数值、可用性与业务条件都由对应领域 owner 校验后注入，UI 只负责排版。


## 6. CG大厅｜只读收藏

适用于玩家明确输入“CG大厅 / 图鉴 / 收集 / 某某的CG”等收藏查看请求。打开CG大厅只读，不推进游戏世界时间，不改变状态，也不自动跳入篇章。

### 6A. 总览页

推荐渲染结构：

```markdown
### 【CG大厅】

收集进度：**<已收录CG数> / <CG总数>**

<角色名A> **<已收录> / 5**
<角色名B> **<已收录> / 5**
...

**查看：** 全部｜按角色
```

渲染细则：

1. 总览页只显示总收集进度与每名角色的已收录数量，保持紧凑。
2. 总CG数、每名角色的章节数与角色列表由篇章 owner 注入；已收录数量由当前工作态注入。
3. 总览页不显示获得方式、首次时间、来源文件、隐藏门槛或剧透说明。

### 6B. 单角色页

推荐渲染结构：

```markdown
──── <角色名> CG ────
收集：**<已收录> / 5**

01 ✦《<已解锁标题>》
   <这张CG的直接画面描写；重点写空间、人物相对位置、物件、动作、表情与可定格的瞬间>

02 ▒ 未解锁
03 ▒ 未解锁
04 ▒ 未解锁
05 ▒ 未解锁
```

渲染细则：

1. 已收录条目直接在列表里显示**标题 + CG画面描写**；这就是完整收藏内容，不再进入二级查看页，也不提供额外操作入口。
2. CG描写必须是**画面内容本身**，不是剧情总结、人物关系总结或系统说明；优先写可见构图、动作、表情、距离、物件与环境。
3. 未解锁条目统一显示 `▒ 未解锁`，不显示标题、条件或画面内容。
4. CG大厅不提供 `[查看CG]`、`[重看场景]` 或等价二级操作；收藏页本身即为最终展示层。
5. 读取边界保持最小必要：总览优先只读当前工作态与公开篇章索引；单角色页只读取该角色当前需要显示的篇章信息与对应CG内容。
6. 所有“是否已收录、标题、画面描写”都由当前 route 从状态 owner 与篇章 owner 取值后注入；本UI只负责排版。
