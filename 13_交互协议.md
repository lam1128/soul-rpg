# 魂师修炼RPG｜交互与输出协议

> **文件职责：** 玩家阶段输入语义、由本文件负责的控制意图 recognition、固定选择输入边界与通用输出节奏。
>
> **权威范围：** 已登记为本文件 recognition owner 的输入语义，以及 route 命中后“当前应等待什么输入、如何把已结算结果呈现给玩家”；普通 route 的优先级与 activation 表只属于 manifest。
>
> **读取策略：** `SELECTIVE_ON_MATCH`，只读当前阶段/意图章节。

## 1. 回复密度

默认简短直接。普通动作不写长篇总结；复杂战斗或重要篇章可以自然展开。具体叙事文字仍读取 `02`。

## 2. CHAR_CREATE

`NEW_GAME` recognition：用户明确要求“开始新游戏 / 重开 / 新建冒险”等创建新的冒险状态时命中；继续既有冒险不属于本项。

固定交互顺序：

1. `NAME`：询问世界内名字；
2. `MARTIAL_SOUL`：展示 `10A` 当前可选模板；
3. `REVIEW`：从领域主源与待提交新游戏状态展示开局公开信息；
4. `GUIDE`：显示本文件 §2A 的玩家操作说明；
5. `DONE`：按 `12` 提交角色创建完成状态并进入世界。

不得在交互层另写一套开局数值或关系常量。

## 2A. PLAYER_GUIDE｜玩家操作说明

`HELP_READONLY` recognition：用户明确询问“怎么玩 / 帮助 / 我能做什么 / 游玩指南 / 主页面 / 游戏主页 / 总菜单 / 导航 / CG大厅 / 图鉴 / 收集”等操作说明或导航界面时命中。

当用户输入“主页面 / 游戏主页 / 总菜单 / 导航 / 我现在能做什么”等明显导航请求时，`readonly_query` 应读取 `UI_统一规范_日常恢复与菜单.md` 的“游戏主页面”模板，并按当前工作态及对应领域 owner 注入玩家可见信息。主页面使用紧凑、正向、可执行的短句，只展示当前最相关入口；打开主页面只读，不推进游戏世界时间，不改变状态，也不替玩家自动选择行动。

当用户输入“CG大厅 / 图鉴 / 收集 / 看看某人的CG”等收藏查看请求时，`readonly_query` 应读取同一UI文件的“CG大厅”模板，并按当前工作态及篇章 owner 注入已收录数量、标题与画面描写。CG大厅只读，不推进游戏世界时间，不改变状态。

本节是唯一静态帮助入口，只说明**怎么操作**，不复制业务常量：

- 开放世界可以自由输入对话、旅行、社交、游戏内修炼、猎魂、等待、拒绝等行动。
- 现实活动支持学习/修炼、工作、睡眠的开始、结束与明确补记；具体奖励和状态影响读取现实活动 route 的领域主源。
- 战斗中可以输入普通攻击、已拥有魂技、使用合法物品、撤退或现场允许的特殊行动；是否合法与如何结算读取战斗主源。
- 伙伴是自主 NPC，玩家可以提出建议、请求和计划，但不逐个接管伙伴回合。
- 猎魂与魂环获得必须走正式猎魂流程；候选、风险、最后击杀与吸收规则读取猎魂/成长主源。
- 伙伴篇章只在当前单章故事卡登记的关键选择点停下来等待 A/B/C；普通拿取、搬动、短程移动、跑腿等低价值动作按故事卡自动叙事推进，不额外制造确认轮。进入/继续篇章必须先用 `C00#1A` 与 manifest `companion_episode_files` 解析唯一单章文件，选项行为语义、隐藏分值、局部分流、旗标与结局只读该单章文件；不接受自由输入临时创建第四分支。
- 玩家对世界结果的直接宣告只是输入，不会绕过机械、关系、经济或世界 owner。
- “查看状态/人物卡/伙伴/篇章状态/CG大厅”进入统一 `readonly_query` 只读 route；“存档”直接交给 Save Manager Skill 的正式 checkpoint 导出事务。
- “怎么认识新朋友 / 去哪里社交 / 怎么遇到新伙伴”等可发现性问题仍属于 `HELP_READONLY`；只读 route 必须从 `11_世界与NPC.md` 取得当前玩家可见的社交/见面规则与线索，并区分“林昭宁原本认识”与“玩家本局已经见过”；不得在帮助层编造 NPC 日程或保证刷出某人。
- “有什么剧情 / 哪些篇章能玩 / 什么时候解锁下一篇”等篇章导航问题仍属于只读帮助/状态查询；只允许从 `C00#1A` 读取玩家当前可见的标题、前置与解锁条件，禁止读取单章正文。可玩列表必须结合当前关系状态与玩家本局见面状态实时判定。
- “有哪些CG / 某角色收集了多少 / 看看某人的CG”等仍属于只读帮助/状态查询；总览优先只读 `12` 的当前进度与 `C00#1A` 的公开标题索引，单角色页只允许读取该角色当前需要显示的少量章节CG内容，禁止为了CG大厅批量遍历45个单章文件。已收录条目直接显示完整画面描写；未收录条目只显示“未解锁”。CG大厅不提供二级查看页、场景回放、来源、获得时间、隐藏门槛或其他资料页信息。
- 游戏外修改设定、整理文件或检查项目直接交给 Project Maintainer Skill，不作为角色行动。

主页面与帮助可以提供导航入口，但**不拥有行动可用性、社交刷新、篇章门槛、经济、成长或时间规则**。这些内容必须由对应领域 owner 在渲染时注入；如果当前没有可公开线索，就明确显示“暂无新的玩家可见线索”，不能用 DM 信息补齐。

如果玩家追问某项具体数值、时长、价格、门槛或公式，再通过对应 route 读取唯一业务 owner；本帮助不保存这些值。

## 2B. STATUS_READONLY recognition

`STATUS_READONLY` recognition：用户只要求查看当前状态、人物卡、武魂/魂技、伙伴公开资料、伙伴篇章公开进度或CG收集情况，且没有同时要求执行会改变状态的行动时命中。具体可见字段与数据只由 `readonly_query` route 在 `STATUS_READONLY` 条件下读取的 sources 提供。

## 3. ADVENTURE

自由输入有效。交互层只负责解释玩家当前话语的语义与缺失输入，不维护 route 名称、优先级或 activation 条件表；机器分流唯一读取 `manifest.interaction_route_activation`。本文件后续章节仅描述各类交互在进入相应领域后“玩家输入算什么”，不能反向成为第二套路由表。

现实“开始修炼/学习/工作/睡觉”等在 pre-router 已被识别为控制事务；明确游戏内主语/地点/时长的同类行为才留在普通世界。

## 3A. 经济交易交互

玩家已经明确“外出吃饭 / 买药 / 在家做饭”等目标，且不存在必要的关键选择或阻断时，不追加无意义的“要不要现在去”确认：直接进入 `economy_transaction`，由 `E00/11/12` 计算地点、时间、价格、库存与恢复候选，再按日常菜单 UI 展示需要玩家选择的项目。

菜单数据、固定耗时、价格和恢复值不得写在本文件。

## 3B. 恢复交互

“怎么恢复 / 休息 / 调息 / 游戏内睡眠 / 使用已有恢复品”等进入 `recovery`。若当前需要玩家选择恢复方式，展示日常恢复 UI；如果方式已经明确，则直接交给恢复主源结算。

现实“开始睡觉”不属于本节，而由 `REAL_ACTIVITY_CONTROL` 处理。

## 3C. 核心伙伴首次出场关系摘要

核心伙伴首次进入当前冒险的玩家可见正式见面/重逢场景时，可以在正文后显示一条简短关系摘要。摘要只能消费 `11` 的静态关系、当前 `RELATION_STATE` 与 `PLAYER_DISCOVERY_STATE`，不泄露未来篇章、隐藏门槛或 NPC 未知信息。场景是否提交 `MET_IN_PLAY` 或新的 `ACQUAINTED` 由世界状态语义与 `12` 原子提交规则决定，交互层本身不改写关系。

## 3D. NPC明确同意后的直接执行

当玩家向 NPC 提出已经足够具体、可立即执行的日常行动：

- NPC明确同意且没有新的关键选择/安全阻断 → 同一回复继续执行到下一自然节点；
- NPC拒绝、犹豫、提出条件或仍缺关键选择 → 停在回应处等待玩家；
- 不得借“直接执行”替玩家补新的心理、对白或额外目标。

## 4. REAL_ACTIVITY｜现实活动输入

自然语言映射：

- 开始/结束修炼、学习 → `TRAINING_STUDY`
- 开始/结束工作、下班 → `WORK`
- 开始/结束睡眠、起床 → `SLEEP`
- 明确“补记/补录/追记 + 活动类型 + 起止时间” → 相应 RETRO 事务

本文件只负责解析意图与必要时间表达。开始、结束、切换、补记的执行顺序读取 `00A`；倍率/上限读取 `10`；会话、Ledger、防重和提交读取 `12`。

结算输出只显示本次实际经过、实际计奖和结算后必要玩家字段；不得自行生成领域主源没有提交的变化。

## 5. COMBAT

### 开始/查看
按魂兽遭遇 UI 或当前战斗输出模板展示已经由战斗主源确定的公开字段。UI 只负责布局，所有数值来自 `10/12/B00/S00` 等当前 owner。

### 玩家输入
玩家可以用自然语言选择当前合法行动。行动是否合法、是否消耗回合、魂力/伤害/治疗如何变化只读取 `10`；伙伴/敌方后续槽位如何自动续转读取 `00A K4`。

### 伤害与技能展示
每次真实结算后显示必要的最新 HP/魂力；数值展示规则由当前 UI 模板消费已结算结果，不在本文件重复公式。

### 遭遇结束
结束时的世界时间后果读取 `11 §8A`，结束卡读取魂兽遭遇 UI。本文件不复制固定耗时。

## 6. COMPANION_EPISODE

进入篇章后，先读取 `C00#§0` 当前题材通用节点 profile 与 `C00#1A` 当前章公开元数据，再通过 manifest `companion_episode_files[CHAPTER_ID]` **只读取一个单章文件**取得 `固定四场 / OPTION_ORDER_SEED / 当前节点`；然后把当前节点的 `R2/R1/R0` **固定行为语义**写成三条自然、具体、可立即选择的 A/B/C 文案。措辞可以随已成立场景、人物口吻和关系熟度变化，但不得改分值、交换语义、暗示哪项是高分、增加第四选项或借自由输入创建新分支结果。

玩家自由输入可以用于询问、查看或拒绝继续；若输入明确等价于当前 A/B/C 某一项，可以按该项消费，否则不能凭自然语言临时生成 C00 之外的新结果。

节点数量、通用分值/结局算法/首通重放语义读取 `C00#§0`；当前章 `OPTION_ORDER_SEED`、章内关键旗标语义与故事结果只读当前单章文件；恋爱门槛只读 `C00#1B`；当前进度只读/写 `12`。已首通章节的旧世界事实按 C00 的内容重制兼容规则保留，交互层不得用新版故事卡回写过去。章末展示消费 C00 已计算结果，不在交互层维护算法副本。

## 7. 恋爱交互

关系状态变化只能来自当前世界/篇章主源允许的明确玩家选择。交互层负责识别“玩家明确选择了哪个关系选项”，不维护恋爱门槛、排他规则或数值效果。

## 8. 猎魂交互

“找魂兽 / 去某猎魂区 / 继续搜索 / 为自己或伙伴找下一魂环”等进入 `hunting`。交互层只保存玩家目标与明确偏好；区域候选、个体生成、魂环候选、收益与状态冻结分别读取对应领域主源。
